<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        .container {
            width: 100vw;
            height: calc(100vh - 143.64px);
            display: flex;
            justify-content: center;
            align-items: center;

        }



        .signin-form {
            border: 1px solid;
            padding: 35px;
            border-radius: 15px;
            font-size: 25px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to bottom, #3f3f6d, #534ca0, #201a52);
            height: 75%;
            width: 33%;
        }

        input {
            font-size: 23px;
            border-radius: 6px;
            padding: 5px;
            width: 25rem;
            height: 3.5rem;
            transition: all 0.2s linear;
            border: none;
        }

        h3 {
            margin-bottom: 30px;
            color: rgb(255, 255, 255);
            font-weight: 600;
        }

        button {
            font-size: 25px;
            padding: 6px;
            border-radius: 5px;
            background-color: #7f71c4;
            color: white;
            box-shadow: 1px 1px 2px rgba(0, 0, 0, 0.4);
            border: none;
            position: relative;
            bottom: 25px;
            transition: all linear 0.2s;
            cursor: pointer;
        }

        button:hover {
            transform: scale(1.06);
        }

        input:hover {
            transform: scale(1.06);
        }

        form {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        p {
            font-size: 20px;
            color: rgb(240, 233, 221);
        }

        .signup {
            font-size: 15px;
            text-decoration: underline;
            color: rgb(240, 233, 221);
        }

        .error {
            display: flex;
            width: 100vw;
            justify-content: center;
            align-items: center;
            border: 1px solid rgb(232, 35, 35);
            margin-top: 10px;
            padding: 10px;
            font-size: 25px;
            color: red;
            background: linear-gradient(135deg, #FCC2BF, #F7BED5);
        }
    </style>
</head>

<body>
    <%- include("./partials/nav") %>

        <% if (locals.error) { %>
            <div class="error">
                <%= locals.error %>
            </div>
            <% } %>

                <div class="container">
                    <div class="signin-form">
                        <h3>Login</h3>
                        <form method="post" action="/user/signin">
                            <input type="text" name="email" placeholder="Email"> <br><br>
                            <input type="password" name="password" placeholder="Password"> <br><br>
                            <button type="submit">Submit</button>
                        </form>
                    </div>
                </div>
</body>

</html>